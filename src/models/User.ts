

export interface User {
  userId: number;
  userName: string;
  userPwd: string;
  userDisplayName: string;
  userTypeId: number;
  userType: any; // You can replace 'any' with the actual type if known
  mandirId: number;
  mobileNo: string;
  emailId: string;
  isSuperAdmin: boolean;
  crtUser: string;
  
  modUser: string;
 
 
}
